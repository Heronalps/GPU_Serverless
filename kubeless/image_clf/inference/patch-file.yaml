spec:
  template:
    spec:
      containers:
      - name: image-clf-inf
        resources: 
          requests: 
            memory: 32Gi      
            nvidia.com/gpu: 2
          limits: 
            memory: 64Gi
            nvidia.com/gpu: 2
        volumeMounts:
        - mountPath: /racelab
          name: imageclf-ceph
      restartPolicy: Always
      volumes:
        - name: imageclf-ceph
          persistentVolumeClaim:
            claimName: imageclf-ceph