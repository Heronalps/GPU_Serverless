spec:
  template:
    spec:
      containers:
      - name: image-clf-train
        volumeMounts:
        - mountPath: /racelab
          name: imageclf-ceph
      restartPolicy: Always
      volumes:
        - name: imageclf-ceph
          persistentVolumeClaim:
            claimName: imageclf-ceph